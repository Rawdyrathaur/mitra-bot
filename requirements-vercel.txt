# Minimal dependencies for Vercel serverless deployment
# This reduces build memory consumption

# Flask Core
Flask==2.3.3
Flask-CORS==4.0.0
Flask-JWT-Extended==4.5.3
Flask-Limiter==3.5.0

# Database (use serverless-friendly options)
psycopg2-binary==2.9.9
redis==5.0.1

# OpenAI
openai==1.3.5

# Document Processing (minimal)
PyPDF2==3.0.1
python-docx==1.1.2
BeautifulSoup4==4.12.2

# Authentication
bcrypt==4.1.2
PyJWT==2.8.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0

# Remove heavy dependencies that cause OOM:
# - langchain (too large)
# - sentence-transformers (uses PyTorch - very large)
# - numpy/pandas/scipy (large scientific packages)
# - pdf2image, pytesseract (OCR dependencies)
